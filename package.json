{
  "name": "contract-operations",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "start:network": "HOST=0.0.0.0 PORT=4182 node server.mjs",
    "check": "node --check server.mjs && node --check app.js && node --check scripts/smoke-test.mjs && node --check scripts/load-test.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "load-test": "node scripts/load-test.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
